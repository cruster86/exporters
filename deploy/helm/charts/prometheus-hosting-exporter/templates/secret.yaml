apiVersion: v1
data:
  password: {{ .Values.global.auth}}
  user: {{ .Values.global.user}}
kind: Secret
metadata:
  name: {{ .Values.serviceMonitor.basicAuth.password.name }}
  namespace: {{ .Release.Namespace }}
type: Opaque
